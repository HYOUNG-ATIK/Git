## Start :runner:

1. Git 설치, Github 계정 및 Repository 생성
1. Local 저장 경로 설정 및 commit
    ```
    cd C:\Users\User\Desktop\Folder
    git init
    git add .
    git commit -m "First commit"
    ```
1. Local 저장소와 원격 Repository 연결
    ```
     git remote add origin "http://github.com/~~(실제 주소)"
    ```



